name: Build and Deploy Docusaurus Documentation

on:
  push:
    branches: [ main ]  # or your default branch
    paths:
      - 'my-docs/docs/**'
      - 'my-docs/docusaurus.config.js'
      - 'my-docs/package.json'
      - '.github/workflows/documentation.yml'

  workflow_dispatch:  # 👈 allows manual trigger from GitHub UI

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20  # 👈 upgraded to Node.js 20 (Docusaurus requirement)

      - name: Install dependencies
        run: |
          cd my-docs
          # Try npm ci if lockfile exists, otherwise fallback to npm install
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build Docusaurus site
        run: |
          cd my-docs
          npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./my-docs/build
          user_name: github-actions[bot]
          user_email: github-actions[bot]@users.noreply.github.com
